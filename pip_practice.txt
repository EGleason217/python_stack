pip install Django==1.11.9 – install, installs packages into environments that can be used for various reasons. pip listLists the packages installed in the environmentdeactivateExits the virtual environmentpip3 listLists the packages in the environment when the environment is not activesource myEnvironments/py3Env/bin/activateactivates the environmentspip install Django==1.11.9the response was that the requirement was already satisfied. pip freezepip freeze shows the version numbers of the packages installed and list shows the packages that are installed. First cd into your Desktop directory (cd ~/Desktop), then run this command: pip freeze > requirements.txt. What do you see when you ls? What's inside this file?It listed all the contents in the home folder. pip uninstall Djangouninstalled Djangopip show Djangodidnt show anything pip search Flaskpip search, searches for packages available to install with that name 